---
interface Props {
    coverURL: string;
}

---

<div
    class="bg-no-repeat bg-cover bg-center fixed w-full h-full"
    style=`background-image: url(${Astro.props.coverURL});`
    id="background"
>
</div>

<div class="backdrop-blur-[128px] bg-primary w-full h-full fixed"></div>
